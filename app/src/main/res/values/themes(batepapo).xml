<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.App" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@color/bgLilac</item>
        <item name="android:navigationBarColor">@color/bgLilac</item>
        <item name="colorSurface">@color/bgLilac</item>
        <item name="colorPrimary">@color/purple</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:textColor">@color/ink</item>
    </style>

    <!-- Tipografia rápida -->
    <style name="Text.Title" parent="TextAppearance.Material3.TitleMedium">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color:purpleText</item>
    </style>
    <style name="Text.Meta" parent="TextAppearance.Material3.BodySmall">
        <item name="android:textColor">@color/muted</item>
    </style>

</resources>